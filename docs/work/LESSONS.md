# Lessons — auto-captured from loop escalations

**This file is THE single canonical intake table for cross-installation lessons** (field
reports, other repos' loop escalations, anything with the shape "we learned X the hard way").
A lesson captured into a different repo's own loose lesson-folder and left unmerged here is
exactly the fragmentation this program's VISION names — port it into a row below (with its
original `Source` tag preserved) as part of landing that work, don't let a second copy stand
in for this one. See `skills/steward/SKILL.md`'s "Cross-installation lesson intake" section
for the merge procedure.

One row per loop failure. `loop-learn.mjs` appends here; `/steward` distills these into the
canonical CLAUDE.md / skills / exemplars instead of cold-starting.

| When | Source | Symptom | Root cause | Rule (do this next time) |
|------|--------|---------|------------|--------------------------|
| 2026-06-23T00:37:26.638Z | anti-drift:wave1 | nearly shipped a redundant/looser (400) file-size gate beside an existing one because an explore agent reported validate-code-health H-02 as 'advisory' | trusted an agent's audit claim ('H-02 advisory') without reading the source; H-02 actually uses gap() = blocking at 250 | an agent's claim that an existing gate is 'advisory/missing' MUST be confirmed by reading the source (grep for gap/exit) before building a parallel mechanism — this is the same perception-drift the anti-drift work targets |
| 2026-07-08 | field-report:mode1-run (A-1/A-6.3) | status rolled up "implementation complete" while ~half the requirement-stories were unbuilt — only the build-task layer had been closed | two work layers (tasks + requirement-stories) with a discipline for closing only one; "wave done" allowed to imply "requirements done" | a phase is NOT complete while its requirement-stories are open — gate on requirement closure, not task closure; run a code↔requirement reconciliation HANDOFF at Phase 4→5 (see IMPROVEMENT_BACKLOG H1) |
| 2026-07-08 | field-report:mode1-run (A-6) | JIRA hierarchy/linkage/labels drifted (phases as sibling stories under one epic, no phase→story link, unlabeled stories invisible to scope math, template strays in counts); had to reverse-engineer + link 150+ stories + reconcile mid-flight | the issue tracker was treated as a place to EMIT work items, not a data model to DESIGN | design the tracker data model once at Phase 2→3: layer map, structural phase→work linkage (not label-only), single source of truth for scope+completion, stray handling; add a tracker-integrity validator at every phase gate (H5) |
| 2026-07-08 | field-report:mode1-run (A-2) | a broad "defer all the X work" comment was applied as a bulk relabel; a launch-critical, only-incidentally-related item got deferred | optimized for executing the instruction over pressure-testing it | a scope/deferral change affecting >1 item must enumerate affected items + get per-item confirmation, flagging anything MVP-load-bearing, BEFORE relabeling (H2) |
| 2026-07-08 | field-report:mode1-run (A-3) | reviewed feature code reached main inside a DOCS pr because the branch was cut while the prior unit's changes were uncommitted in the tree | the system's own "one unit = one branch = one PR" rule wasn't self-enforced at context-switch | assert `git status` clean before `git checkout -b` for a new unit; after any merge `git show --stat` and flag files outside the PR's declared scope (H3) |
| 2026-07-08 | field-report:mode1-run (A-5) | the two most-challenged tech choices (datastore; "vendor the component lib") rested on thin/incorrect recorded rationale — incl. a supply-chain reason that cited the WRONG library — presented as settled | system captured WHAT was decided, not a disciplined WHY; soft reasons (stakeholder convenience, prior-employer habit) promoted into design docs as engineering rationale, some incorrect | load-bearing tech choices need a short ADR (alternatives + actual deciding factors, soft reasons tagged AS soft); route asserted rationales ("X for supply-chain reasons") through Challenger to verify before they enter a design doc (H4) |
| 2026-07-08 | field-report:mode1-run (B-1) | RBAC shipped where creating the FIRST project needed a role only an existing role-holder could grant — app unusable on an empty DB without manual SQL; passed design+build+review, a joining dev hit it on first deploy | designers/reviewers reason about steady state (data+roles exist); nobody's job was t=0 (empty DB, no users/roles, first run) | add a Bootstrap & Empty-State checklist to arch-designer + security-auditor and a Phase-5 fresh-deploy dry-run; add "self-referential permission gate" + "bootstrap authority" to the threat catalog (H6) |
| 2026-07-08 | field-report:mode1-run (B-2) | "vendored" components were AI-written from the library's SHAPE (renamed/dropped variants, old template), not generated from the real tool — a silent fork the docs masked as "we use library X" | told "use library X (copy-paste)", the agent generated X-flavored code from memory; no step verified "is this actually X or X-shaped?" | when a library is vendored/copied, generate it from the library's real CLI/registry (record the version), not from memory; reviewer spot-diffs a sample vs upstream and flags drift as fork/maintenance debt (B-2) |
| 2026-07-08 | field-report:mode1-run (B-3) | data model allowed multiple roles per user but enforcement collapsed to highest-role-wins — a multi-role user couldn't use their lower-precedence role's permissions | RBAC modeled as "the user's role" (singular) at enforcement even though the schema stored a set | reconcile the schema's role cardinality with the enforcement function's: if N roles per principal are storable, enforcement must be union-of-grants ("allowed if ANY held role grants it"), not highest-wins (B-3) |
| 2026-07-08 | field-report:mode1-run (C-1) | a published status page showed all phases green / "ready for QA" while MVP was ~half built; another page was stale the other way | status written once, not re-derived from live data; "phase built" (scaffolding) conflated with "phase done" (features) | status artifacts must derive % live from the tracker, distinguish "platform built" vs "features done", never paint a phase green while its requirement-stories are open, and carry a staleness check (H7) |
| 2026-07-08 | field-report:mode1-run (C-2/C-3) | published diagrams silently fell back to raw text (unsafe char in a node label) and a large table rendered half-raw (blank line split it into a header-less fragment) — shipped unnoticed | "it committed" treated as "it rendered"; no post-publish render verification; append-only tables corrupt over many sessions | add a publish render-health gate (every diagram → image, every table → real table) + a markdown-table orphan-fragment linter + a diagram-syntax linter (unsafe label chars) (H8) |
| 2026-07-08 | field-report:mode1-run (D-1, WIN) | verify-don't-trust caught nearly every serious defect this run (imprecise "no route exists" claim; a vendored-lib default-value trap; genuine-vs-happy-path security invariants) | — (this is the win to codify, not a failure) | make "re-ran independently: <what, counts, exit codes>" a REQUIRED field in every gate score; a score without it is incomplete (H9). Keep "cite file:line not recollection" + "platform-built ≠ product-done" as standing laws |
| 2026-07-12 | amplifier/session:land-T31.4/T31.6 | EVERY amplifier PR conflicted on merge — all touch the shared `EXECUTION_TICKETS.md` board file, so main's board and the PR's board always diverge (had to resolve a conflict per PR) | the work-status ledger and code live in ONE file/PR stream (the T10.6 race, now proven to block hand-merges too, not just concurrent writers) | separate the status ledger from code: feature branches must NOT carry board edits; status lives on its own file/branch written single-writer on main (the plan.json model), or PRs are code-only and the board flip is a separate serialized `--close` on main. This is THE prerequisite for any amplifier automation. |
| 2026-07-12 | amplifier/session:land-T31.4/T31.6 | `launch-executor --close` committed the board flip to Gitea only (its GitHub push failed) → the two remotes split-brained and re-diverged after EVERY close; had to `git merge origin/main` + dual-push twice to reconverge | the tool's dual-remote push is non-atomic: it commits + pushes origin, and on a GitHub fast-forward reject it leaves the commit on Gitea and warns 'will sync next time' | dual-remote board writes must be atomic — push BOTH or roll back; a failed second-remote push is an ERROR that must block further board writes, never a soft 'sync later' warning that lets the next op fork off the stale side. |
| 2026-07-12 | amplifier/session:land-T31.4/T31.6 | the tool reported ticket 'already done' while the human-readable `[...·review]` tag still showed review — two disagreeing truths for one ticket's status | ticket status is BOTH hand-maintained in the prose tag AND tracked separately by the tool | single source of truth for status: render the `[...·<status>]` display tag FROM the tool's status store, never hand-edit both; a divergence between them is a board-integrity gap a validator should catch. |
| 2026-07-12 | amplifier/session:land-T31.4/T31.6 | a full-suite run FAILED on the T31.6 feature branch (skills-parity: live `missingInClaude=[]` vs a non-empty expected set) and I nearly treated it as a real defect — it was a merge-ordering artifact (branch predated main emptying `KNOWN_MISSING_IN_CLAUDE`) | the failing assertion's EXPECTATION lived in source main had already changed; the branch was stale, not broken | bug-fix discipline for branch test failures: when the failing area is unrelated to the branch's change, `git show main:<file>` the assertion's inputs before diagnosing; a branch failure that clears after merging main was never a defect. |
| 2026-07-12 | amplifier/session:land-T31.4/T31.6 (WIN) | suite was 287-passed/1-failed on the source branch but 291-passed/0-failed on main after the merge commit — verifying only pre-merge on the branch would have blocked a good merge on a phantom failure | merge-order-dependent results only appear on the integration target | re-run the FULL suite on main AFTER the merge commit, not just pre-merge on the feature branch; 'green on the branch' and 'green after integration' are different claims and the second is the one that gates a release. |
| 2026-07-13 | amplifier/wave-review:haiku-vs-sonnet | haiku base produced 1 usable PR of 3 (one PR contained ONLY .code-search db files with 0 real code; another rewrote MASTER_PROMPT to call `tickets.mjs`, a tool this repo doesn't have); sonnet base produced 3 of 3 clean | haiku is too weak to self-verify scope/artifacts on non-trivial release tickets — it commits the wrong tree and invents tooling | use sonnet as the FLOOR for release-bound tickets; reserve haiku for trivial mechanical edits only, and gate every wave through an independent verifier (maker≠verifier caught all 3 haiku defects) |
| 2026-07-13 | amplifier/wave-review:masked-CI | fixing one CI failure (shallow-clone `fetch-depth`) un-masked a SECOND failure (build:claude:check drift) that had been hidden because CI steps run sequentially and stop at the first red — 'CI is green' was asserted too early after the first fix | sequential CI stops at the first failing step, so a fix can reveal a previously-invisible failure rather than turn the run green | after fixing a CI red, ALWAYS re-run to completion and confirm success — never assume one fix = green; a moved failure looks identical to a fixed one until you watch the whole run |
| 2026-07-13 | amplifier/wave-review:build-claude-check-false-block | every PR that adds a skill/agent/script showed CI UNSTABLE via `build:claude:check`, because the generated attest-claude is only regenerated AFTER merge — so the check can never pass on the feature branch that introduces the content | a post-merge-regen dual-repo gate run on PR branches is structurally unsatisfiable for any content-adding PR | gate `build:claude:check` on main only (post-merge), or treat its UNSTABLE on content-adding branches as expected; don't block a merge on it — verify `npm test` separately (that IS branch-satisfiable) |
| 2026-07-13 | amplifier/wave-review:sibling-cross-contamination | two parallel executors delivering into the same generated repo (attest-claude) collided on the shared local sibling: T5.6's executor committed its regen into ../attest-claude, which (a) made T26.4's local test run fail spuriously (skills-parity saw a stray vault skill) and (b) led to a STALE regen commit that missed the /vault row, failing CI twice | the generated-repo sibling is shared mutable state; per-executor regen races and leaves partial/stale commits | make attest-claude regeneration a SINGLE post-merge conductor step (regen once after all merges land), never a per-executor action; verify build:claude idempotency (git diff clean) BEFORE committing the regen |
| 2026-07-25 | bench:realworld-local | `opencode run --agent <x>` returned exit 0 and plausible output, but no specialist had run — x was mode:subagent so opencode silently fell back to the DEFAULT agent | a subagent name passed to --agent is a notice, not an error; exit 0 was treated as evidence the requested agent ran | never infer 'the specialist ran' from exit 0 — detect `is a subagent, not a primary agent` in the output and fail loudly (now guarded in tools/task.ts); only mode:primary agents are dispatchable via path B, subagents need path C |
| 2026-07-25 | bench:realworld-local | the same fallback ALSO lost `--dir`: the session read from and wrote SRS.md/USER_STORIES.md into the expert repo itself instead of the target workcopy | opencode's default-agent fallback path does not carry `--dir` through; the harness assumed --dir was honoured because it is on the normal path | when spawning with `--dir`, assert afterwards that the artifacts landed INSIDE the workcopy; treat any write to the parent repo as a failed dispatch, not a result. `cwd:` (as tools/task.ts uses) survives the fallback where `--dir` does not |
| 2026-07-25 | bench:realworld-local | orchestrator agent (sdlc-lead) produced 0 artifacts in 38s when driven as a single unattended session | sdlc-lead is a ROUTER (mode:primary, 44 handoff refs) whose mandatory startup sequence reads scripts/detect-sdlc-state.sh + docs/work/SDLC_AUDIT.md — neither exists in a clean project workcopy, so it found no pipeline state and stopped | never point a programmatic single-shot session at an orchestrator: dispatch the PRODUCER specialist directly. Orchestrators assume repo-local pipeline state and a human to route to |
| 2026-07-25 | bench:realworld-local | local model burned ~9 failed WebFetch calls on hallucinated documentation URLs (nodejs.org/api/date.html, /api/timestamp.html, /api/timerange.html — none exist) before falling back to web_search | the model constructs plausible doc URLs from memory and fetches them, rather than searching first and fetching only URLs it was given | agent guidance for local tiers must say SEARCH BEFORE FETCH: never hand-construct a documentation URL; fetch only URLs returned by a search or resolved via context7. Each guessed URL costs a full round trip and can burn most of a phase's wall-clock |
| 2026-07-25 | bench:realworld-local | citation checker flagged 4 sources as fabricated; 3 were real npm packages (decimal.js, bignumber.js, @js-temporal/polyfill) | 403 from bot-protected hosts (npmjs, gitlab) was collapsed into 'dead link'; a browser UA does not help either | link verification needs THREE outcomes — live (2xx/3xx), blocked (403/429, unverifiable), dead (404/410/000). Only dead is evidence of fabrication; blocked must be reported as unverifiable, never as a model defect |
