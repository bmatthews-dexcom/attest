# Lessons — auto-captured from loop escalations

One row per loop failure. `loop-learn.mjs` appends here; `/steward` distills these into the
canonical CLAUDE.md / skills / exemplars instead of cold-starting.

| When | Source | Symptom | Root cause | Rule (do this next time) |
|------|--------|---------|------------|--------------------------|
| 2026-06-23T00:37:26.638Z | anti-drift:wave1 | nearly shipped a redundant/looser (400) file-size gate beside an existing one because an explore agent reported validate-code-health H-02 as 'advisory' | trusted an agent's audit claim ('H-02 advisory') without reading the source; H-02 actually uses gap() = blocking at 250 | an agent's claim that an existing gate is 'advisory/missing' MUST be confirmed by reading the source (grep for gap/exit) before building a parallel mechanism — this is the same perception-drift the anti-drift work targets |
| 2026-07-08 | field-report:mode1-run (A-1/A-6.3) | status rolled up "implementation complete" while ~half the requirement-stories were unbuilt — only the build-task layer had been closed | two work layers (tasks + requirement-stories) with a discipline for closing only one; "wave done" allowed to imply "requirements done" | a phase is NOT complete while its requirement-stories are open — gate on requirement closure, not task closure; run a code↔requirement reconciliation HANDOFF at Phase 4→5 (see IMPROVEMENT_BACKLOG H1) |
| 2026-07-08 | field-report:mode1-run (A-6) | JIRA hierarchy/linkage/labels drifted (phases as sibling stories under one epic, no phase→story link, unlabeled stories invisible to scope math, template strays in counts); had to reverse-engineer + link 150+ stories + reconcile mid-flight | the issue tracker was treated as a place to EMIT work items, not a data model to DESIGN | design the tracker data model once at Phase 2→3: layer map, structural phase→work linkage (not label-only), single source of truth for scope+completion, stray handling; add a tracker-integrity validator at every phase gate (H5) |
| 2026-07-08 | field-report:mode1-run (A-2) | a broad "defer all the X work" comment was applied as a bulk relabel; a launch-critical, only-incidentally-related item got deferred | optimized for executing the instruction over pressure-testing it | a scope/deferral change affecting >1 item must enumerate affected items + get per-item confirmation, flagging anything MVP-load-bearing, BEFORE relabeling (H2) |
| 2026-07-08 | field-report:mode1-run (A-3) | reviewed feature code reached main inside a DOCS pr because the branch was cut while the prior unit's changes were uncommitted in the tree | the system's own "one unit = one branch = one PR" rule wasn't self-enforced at context-switch | assert `git status` clean before `git checkout -b` for a new unit; after any merge `git show --stat` and flag files outside the PR's declared scope (H3) |
| 2026-07-08 | field-report:mode1-run (A-5) | the two most-challenged tech choices (datastore; "vendor the component lib") rested on thin/incorrect recorded rationale — incl. a supply-chain reason that cited the WRONG library — presented as settled | system captured WHAT was decided, not a disciplined WHY; soft reasons (stakeholder convenience, prior-employer habit) promoted into design docs as engineering rationale, some incorrect | load-bearing tech choices need a short ADR (alternatives + actual deciding factors, soft reasons tagged AS soft); route asserted rationales ("X for supply-chain reasons") through Challenger to verify before they enter a design doc (H4) |
| 2026-07-08 | field-report:mode1-run (B-1) | RBAC shipped where creating the FIRST project needed a role only an existing role-holder could grant — app unusable on an empty DB without manual SQL; passed design+build+review, a joining dev hit it on first deploy | designers/reviewers reason about steady state (data+roles exist); nobody's job was t=0 (empty DB, no users/roles, first run) | add a Bootstrap & Empty-State checklist to arch-designer + security-auditor and a Phase-5 fresh-deploy dry-run; add "self-referential permission gate" + "bootstrap authority" to the threat catalog (H6) |
| 2026-07-08 | field-report:mode1-run (B-2) | "vendored" components were AI-written from the library's SHAPE (renamed/dropped variants, old template), not generated from the real tool — a silent fork the docs masked as "we use library X" | told "use library X (copy-paste)", the agent generated X-flavored code from memory; no step verified "is this actually X or X-shaped?" | when a library is vendored/copied, generate it from the library's real CLI/registry (record the version), not from memory; reviewer spot-diffs a sample vs upstream and flags drift as fork/maintenance debt (B-2) |
| 2026-07-08 | field-report:mode1-run (B-3) | data model allowed multiple roles per user but enforcement collapsed to highest-role-wins — a multi-role user couldn't use their lower-precedence role's permissions | RBAC modeled as "the user's role" (singular) at enforcement even though the schema stored a set | reconcile the schema's role cardinality with the enforcement function's: if N roles per principal are storable, enforcement must be union-of-grants ("allowed if ANY held role grants it"), not highest-wins (B-3) |
| 2026-07-08 | field-report:mode1-run (C-1) | a published status page showed all phases green / "ready for QA" while MVP was ~half built; another page was stale the other way | status written once, not re-derived from live data; "phase built" (scaffolding) conflated with "phase done" (features) | status artifacts must derive % live from the tracker, distinguish "platform built" vs "features done", never paint a phase green while its requirement-stories are open, and carry a staleness check (H7) |
| 2026-07-08 | field-report:mode1-run (C-2/C-3) | published diagrams silently fell back to raw text (unsafe char in a node label) and a large table rendered half-raw (blank line split it into a header-less fragment) — shipped unnoticed | "it committed" treated as "it rendered"; no post-publish render verification; append-only tables corrupt over many sessions | add a publish render-health gate (every diagram → image, every table → real table) + a markdown-table orphan-fragment linter + a diagram-syntax linter (unsafe label chars) (H8) |
| 2026-07-08 | field-report:mode1-run (D-1, WIN) | verify-don't-trust caught nearly every serious defect this run (imprecise "no route exists" claim; a vendored-lib default-value trap; genuine-vs-happy-path security invariants) | — (this is the win to codify, not a failure) | make "re-ran independently: <what, counts, exit codes>" a REQUIRED field in every gate score; a score without it is incomplete (H9). Keep "cite file:line not recollection" + "platform-built ≠ product-done" as standing laws |
