# Release tracker — bpm-opencode-experts (FIRST in the release order)

**Shipped:** v2.0.0 (2026-07-12) — field-lessons fold, M28 conductor ref impl, exact parity.
**Target:** **v2.1.0 "v2 stream complete"** — every opencode-experts-related amplifier ticket
done+merged, local-model work finished, conductor adapted to the module schema. Shipwright
stays paused until this ships (founder decision 2026-07-12). Alignment: `docs/ALIGNMENT_MATRIX.md`.

## R1 — Amplifier M26–M31 stream (the bulk; AUTOMATED)
The 42-ticket go-forward stream (T26 lifecycle, T27 gate integrity, T28 conductor, T29 field
lessons, T30 advisor/tier-guard, T31 self-audit). State 2026-07-12: 12 done / 17 ready / 13 blocked.
- **Automation (RUNNING):** `bpm-agent-amplifier/scripts/conductor.mjs` under
  `supervise-conductor.sh`, scoped `--match "T2[6-9]|T3[01]"` — one `claude -p` executor per
  ticket, limit sleep-to-resume, tickets land in **review**, halts if this repo's suite goes RED.
- **Human loop:** morning queue — review PRs (`gh pr list`), merge, then
  `node scripts/launch-executor.mjs --close-merged` on the amplifier board; blocked tickets
  unblock as their deps merge (re-run staleness check).
- Exit: all T26–T31 non-waived tickets done; waivers signed in the board.

## R2 — Autonomy O2 + O3 (local-model autonomy proof)
From AUTONOMY_AND_LOOP_UPGRADE_PLAN: **O2 loop upgrades** (fold the v2 tier-aware budgets into
run-until-done/run-plan runtime behavior, not just protocol text) and **O3 prove** (live numbers
on local hardware per O3_PROVE_RUNBOOK — qwen3.6/gemma-QAT runs demonstrating soft-gates +
12-iteration convergence loops + escalation ledger). This is the "works better with local models"
finish line: measured, not asserted.

## R3 — Conductor adaptation to module tickets (T28.x completion)
`scripts/conductor/` (field-proven, plan.json product boards) adapted to THIS repo's schema:
claim/close via `tickets.mjs` verbs (not raw status flips) · gates via `run-handoff-gates.sh` ·
per-role models.json honored · finding-ledger classes at the review step. Acceptance: conductor
runs a 3-ticket fixture board end-to-end with red fixtures proving spoofed manifest/self-accept/
promise-token all refused.

## R4 — Finding-ledger classes scripted (close the protocol→script gap)
`fix-verify.mjs` today prints CLOSED/STILL-OPEN/NEW. Add: **REGRESSED** detection (fingerprint
reappears after CLOSED), per-row attempt counters, and iteration-class output
(STALLED/PROGRESSED/OSCILLATING) so FIX_VERIFY step-5 budgets are computed, not judged.
Tier-aware ceilings read from `.model-context` tier. Tests: fixture sequences for each class.

## Test truth (release gate for v2.1.0)
1. `npm test` — full suite (283 at v2.0.0; grows with R3/R4) — 0 failures.
2. `node scripts/check-validator-fixtures.mjs` — every chained validator red/green fixtured.
3. `npm run evals` — eval harness green on the fixture set.
4. **O3 evidence** — local-model run artifacts committed (the prove-runbook outputs).
5. `npm run build:claude:check` — 0 drift; skills-parity exact.

## Release steps (repeatable)
`npm test` green → `npm version 2.1.0` → `npm run build:claude` → commit both repos →
tag `v2.1.0` both → push `origin`+`github` `--tags` both → `gh release create` both →
re-run Shipwright `scripts/import-content.mjs` (SW-R1) so the product ships the same library.

## Status log
- **2026-07-23 — v2.27.0 RELEASED** (both repos; 510 tests green): **coding-agent verify loop must converge to GREEN, not "ran".** Field trace (T-164, lead=Sonnet 5, coder=gpt-5-mini): handoff layer worked (agent executed, no re-print) and the lead's independent §2b verification caught everything (3/10 REVISE) — but the coder committed BEFORE any test, appended `|| true` to the verbatim verify commands, replaced a 335-line test file with a 20-line stub (net −26 tests vs main), left 15 biome errors, and reported a curated snippet masking its own failures. Root repo-side gap: the 'Edit, don't rewrite' protection was small-tier-only and tiering is by CONTEXT SIZE, so a cloud mini (400k ctx) got zero weak-model protections while failing exactly like one. Mechanical fixes (counts/exit codes, not self-monitoring): coding-agent Phase 1 captures a BASELINE suite count before any edit (red baseline → BLOCKED); Phase 4 rewritten as a convergence loop (read every exit code + summary line; no `|| true`/suppression — appending it IS paraphrasing; post-change count ≥ baseline + additions or self-reject and restore; no commit until green; report LITERAL final summary lines — 'truncated'/curated excerpts void); Law 5 'Edit, don't rewrite' promoted to ALL tiers (a shrinking test file is a stop-and-reread signal); BOUNDED_TASK_CONTRACT **Rule 12** ('Verify means GREEN, not ran') binds every specialist and makes completion-phrase-over-red-gate a contract violation (auto-reject grounds); MODEL_ADAPTER capability-floor note — mini/flash/haiku-class cloud models get small-tier BEHAVIOR rules regardless of context-derived tier.
- **2026-07-23 — v2.26.1 RELEASED** (bpm-opencode-experts only — scripts are source-only; 510 tests green): two bugs found by provisioning a real zsh dev box (192.168.13.15). **check-tools.sh misreported installed tools as FAILED/missing:** success was judged by `rc==0 && command -v`, but pipx exits non-zero on 'already installed' and `npm i -g` lands in ~/.npm-global/bin which isn't on the current PATH — so successful installs printed FAILED. Now judged by the ARTIFACT (`tool_path` resolves via PATH then known bins: ~/.local/bin, npm prefix bin, ~/.npm-global/bin, ~/go/bin); the presence report uses it too (off-PATH tool shows present with an 'add its dir to PATH' note). PATH hint was hardcoded to ~/.bashrc → now `login_profile` names the login shell's rc (.zshrc/.bashrc/.profile). **setup-dev-server.sh wrote LM_STUDIO_URL to the wrong file:** shell detection keyed on `$ZSH_VERSION` (always empty — the script runs under bash), so it always picked ~/.bashrc even on a zsh box where the interactive session never reads it. Now detects the LOGIN shell and writes one idempotent block to the correct rc, and also persists ~/.npm-global/bin on PATH. Container proof gains a re-run case (tool installed off-PATH → reported INSTALLED, PATH hint at its bin). The .15 box is fully provisioned + verified: opencode sees all 22 lmstudio-remote models; all 8 tools resolve in interactive zsh.
- **2026-07-23 — v2.26.0 RELEASED** (both repos; 510 tests green): three folded fixes. **(1) HANDOFF intake made universal (audit + challenge).** Verified the v2.23.0 intake fix is universal: the block is present+current in all 65 handoff-receiving agents (validator-gated), and a live gpt-5-mini pointer handoff to db-architect — block only, no extra wiring — executed and printed its exact phrase, proving the block's authority wins on its own. Then found+fixed the latent gap: 9 agents (api-designer, challenger, code-reviewer, container-ops, db-architect, git-expert, sre-engineer, test-engineer, ui-verifier) still had per-agent Bounded-Task gates keyed ONLY on the literal `SDLC-TASK for` prefix, contradicting the intake block; both gate phrasings now also recognise a `docs/work/HANDOFF_*.md` pointer. New `prefix-only-mode-gate` validator check (red on revert). **(2) Compaction fix corrected from a real TUI trace.** A captured TUI autocompaction proved the resume-anchor plugin FIRES in the TUI (its text was verbatim in the summary) but v2.25.0's multi-handoff guard punted in the common case (5 handoff files present), so a mid-task coding-agent drifted to a menu ("Which should I do now?") — the reported 'gets lost'. Fix: rank handoffs by newest-PRODUCE-file mtime to name the active one (hedged so a fan-out session can override), and when a PRODUCE file is owed the anchor now FORBIDS the menu/ask-user drift ("You are MID-TASK … do NOT present a menu … CONTINUE it"); compaction-summary instruction gained the matching 'never conclude nothing-outstanding while a file is missing' rule. Reproduced against a 5-handoff fixture. **(3) Remote dev-server provisioner.** `scripts/setup-dev-server.sh`: one command to install the expert system on a Linux dev box and point opencode at a local-LLM host (default 192.168.13.179:1234); re-running is a refresh (pull + reinstall + reconfigure). Never-sudo; merges the provider block with a LIVE model list from the host, preserving install.sh's mcp/permission blocks; backs up config first; outage-safe. Pass 33e static + behavioural merge proof. Plugin/script are source-only (not in the generated set); claude-experts carries only the agent gate-fix. 502 → 510 tests.
- **2026-07-23 — v2.25.0 RELEASED** (both repos; 503 tests green): **Long runs survive autocompaction.** Field report (gpt-5-mini): on a long review/coding run opencode autocompacts — replaces history with a summary — and a weaker model loses the thread (redoes finished work, forgets owed PRODUCE files, drops the completion phrase). Existing protocol (CHECKPOINT_STATE + the 70% rule) asks the *model* to self-checkpoint, the same self-monitoring a small model doesn't do — so this is deterministic. **Principle:** disk state survives compaction; history doesn't — so recompute a short 'where am I' anchor from the filesystem and re-inject it on EVERY request (a post-compaction turn gets the same anchor as any other, because it never depended on history). **`plugins/resume-anchor.ts`**, two hooks: `experimental.chat.system.transform` (load-bearing) injects the active `docs/work/HANDOFF_*.md`, each PRODUCE file marked `[done]`/`[MISSING]` by real `existsSync`, the exact completion phrase, `STATE.md`'s Next step, and any `phaseN.md` written; `experimental.session.compacting` (best-effort) appends must-survive pointers, never replaces the default prompt. **Parallel fan-out guarded:** `/review` writes 3 HANDOFFs into one `docs/work/`, so newest-by-mtime would hand a `/security` session the wrong PRODUCE list + phrase, confidently — with >1 HANDOFF the anchor lists paths and asserts NO phrase. Emits nothing on non-SDLC projects; kill switch `EXPERTS_RESUME_ANCHOR=0`. **Prompt backstop:** BOUNDED_TASK_CONTRACT **Rule 11** — write each finished unit to its PRODUCE file the moment it's done, never batch to the end, so the anchor has rich disk state to reload. **Verification (stated precisely):** `chat.system.transform` firing + anchor reaching the model is PROVEN live on gpt-5-mini (tools forbidden, it named the MISSING PRODUCE file + exact phrase, fresh session and after 8 turns); hook bodies + fan-out guard PROVEN (Pass 33d). Behaviour ACROSS a real compaction is UNPROVEN — opencode has no manual compact command and autocompaction wouldn't trigger in 8 turns (prune:false, reserved=40000); 'survives compaction' rests on the architecture, not a measurement, and is flagged as such in the plugin header. Also documented the compaction knobs (SETUP.md + examples/opencode.json): `prune` silently drops old tool outputs; `tail_turns` default 2 is low for multi-step work; a large `reserved` wedges the session (gpt-5-mini hung at 118000-250000 vs 128k input). 497 → 503 tests.
- **2026-07-23 — v2.24.0 RELEASED** (bpm-opencode-experts only — claude-experts unchanged, check-tools.sh is not in the generated set; 497 tests green): **`check-tools.sh` works on a bare non-root Linux box.** Field report (Ubuntu 24.04 noble, node from nodesource, non-root): every auto-install printed a bare `FAILED <tool>` with no reason and the hints were macOS-only, so the causes had to be reverse-engineered by hand. Reproduced byte-for-byte in a bare `ubuntu:24.04` container as a non-root user. Four defects: (1) **errors were swallowed** — `eval "$cmd" >/dev/null 2>&1` — the one that cost the debugging session; failures now print the real stderr tail. (2) **`npm i -g` EACCES** on a root-owned global prefix (nodesource + distro Node both do this) now retries scoped via `npm i -g --prefix ~/.npm-global` — deliberately NOT `npm config set prefix`, which would silently rewrite the user's npmrc — then prints the PATH line; knip/ts-prune/jscpd install with no sudo. (3) **pipx absent + PEP 668** externally-managed Python (so `pip install --user pipx` is refused too) — print the one accurate distro command and say why the obvious workaround fails, rather than guessing. (4) **mmdc de-listed from auto-install** — `@mermaid-js/mermaid-cli` pulls puppeteer (Chromium download, needs `unzip` + browser libs) and `PUPPETEER_SKIP_DOWNLOAD` leaves a *broken* renderer, strictly worse than absent since `validate-mermaid.sh` cleanly skips a missing mmdc; a half-finished download also poisons every retry, so the hint says to clear `~/.cache/puppeteer`. Hints now resolve per-OS (`brew install trufflehog` was being shown to Linux users; trufflehog stays — secrets-scanner invokes it — the hint was wrong, not the tool). **Never-sudo contract preserved and made explicit:** the script prints privileged commands and never runs them. Guards: **Pass 33c** (6 static invariants) + `scripts/test-check-tools-container.sh` (10-assertion behavioural proof on a bare ubuntu:24.04 image); all three planted regressions confirmed caught. 490 → 497 tests. **CI deliberately does not gate this** — GitHub's `ubuntu-latest` has a writable npm prefix and `unzip`, so it reproduces none of it and a green run would be false comfort.
- **2026-07-23 — v2.23.0 RELEASED** (both repos, tags ancestors of main, GH + Gitea releases; regen-before-push, 490 tests green): **HANDOFF intake — pointer-delivered handoffs execute instead of being handed back.** Field report on `gpt-5-mini` via opencode: `/code` and `/review-code` given a HANDOFF re-printed it and told the user to run the client they were already running, producing zero files. Reproduced against released v2.22.0 with the wording the orchestrator actually emits (`"open /review-code, it reads docs/work/HANDOFF_code-reviewer.md"`). **Root cause:** Bounded Task Mode triggers only on a prompt *starting with* `SDLC-TASK for`, but interactive delivery (EXECUTOR_SELECTION Executor C, the default) hands over a *pointer*, which never matched — so coordinators fell through their mode table to Orchestrator Mode, whose job is to emit handoffs. Strong models inferred the intent; weaker ones followed the table. **Fix, both sides:** new mandatory `HANDOFF intake` block (`blocks/handoff-intake.md` + compact, registered in build-agents.mjs) in all **65** handoff-receiving agents, ahead of mode selection and scope-boundary checks — a pointer to a HANDOFF *is* a HANDOFF, never re-emit one you received, `USER:` lines are not addressed to you, never end a turn asking which mode/slug to run, HANDOFF paths are project-relative (gpt-5-mini resolved `/docs/work/...` → denied read → zero output); and the emitter now prints a paste line *starting with* `SDLC-TASK for`, so the backstop isn't load-bearing. **Resolved a read-vs-paste contradiction shipped in v2.22.0:** HANDOFF_QUICK_REF / SESSION_PRIMER / LOCAL_LLM_PRIMER / `review` + `reflow` skills baked `USER: open a new session… paste EVERYTHING below` **inside** the `════` delimiters — inside the body the specialist reads as its task — while HANDOFF_TEMPLATES said the opposite ("a DOCUMENT the specialist reads, not a block the user pastes"); MODEL_ADAPTER routes `tier=small` to QUICK_REF, so the paste-model header hit exactly the models least able to ignore it. **Build fix:** `build-agents.mjs` enumerated only top-level `agents/*.md`, so every shared-block edit silently skipped the 50 cluster agents under `code-review/`, `security/`, `performance/`, `game/`, `sdlc/onboard/` while `--fix` reported "in sync" — `--check`/`--fix` now walk recursively (46 → 96 agents); `--compact` stays top-level (flat overlay). **Regression guards:** two new checks in `validate-handoff-discipline.sh` (`user-line-in-handoff-body` — any `USER:` line at or below the opening `════`, across agents/ and skills/; `missing-handoff-intake` — any agent referencing `SDLC-TASK for` without the block) + **Pass 33b** (`test-handoff-intake.ts`) driving both through the real validator against red/green fixtures, asserting the block is present *and current* in every receiver including subdirs, and asserting both canonical emitters print a trigger-prefixed paste line. Verified end-to-end on `gpt-5-mini`: before → 0 files + "tell me which mode to run"; after → handoff read, PRODUCE file written, Completion Manifest, verbatim completion phrase. Leaf (`coding-agent`) and no-handoff orchestrator paths re-checked for regressions.
- **2026-07-18 — v2.22.0 RELEASED** (both repos, tags ancestors of main, GH + Gitea releases; regen-before-push, 487 tests green): **Opengrep-first SAST migration** (licensing). Semgrep-maintained registry rules (`--config auto`, `p/*`) are internal-use-only — barred from client audits / SaaS. Switched engine to **Opengrep** (LGPL fork, CLI/rule-format compatible, free intra-file cross-function taint) + rule source to in-house **bpm-rulepacks**. `tools/semgrep-scan.ts`/`semgrep-rule.ts` resolve opengrep→semgrep (`SAST_ENGINE`/`RULEPACKS_DIR`); `semgrep-full-audit.sh` defaults to bpm-rulepacks, gates registry + AGPL-community caches behind `--dev-registry`; `update-semgrep-rules.sh` DEV-ONLY banner; `doctor.sh` prefers opengrep + ast-grep, trufflehog→gitleaks; Semgrep Runner agent + security skill reworded; `install.sh --opengrep` (official curl installer, no brew). Also: web-research repointed at renamed **Quarry** (bpmforge/quarry) with bpm-pull/playwright-search aliases (no migration). Part of the CPU-First Offload program (bpm-market-strategy/programs/cpu-first-offload).
- **2026-07-14 — v2.12.0 RELEASED** (both repos, tags ancestors of main, GH releases live; regen-before-push, 421 tests green): **Memory read-loop + lifecycle** (memory+code-search program part 2/2 — COMPLETES it). Audit: memory write-heavy/read-only (82 store vs 2 recall; M5 hygiene documented-never-executed; feedback/consolidate/goal_anchor/update/link 0-wired). **Engine verified capable FIRST** (bpm-memory-mcp mcp/memory-server/src/index.ts: memory_feedback helpful/wrong/outdated/duplicate + memory_consolidate merge-dupes+decay->30d are real) so wiring isn't cosmetic. Wired: **memory_feedback after recall** in sdlc-lead Step 2b (MEMORY_PRIMER M4b — the direct fix for 'memory made coverage worse': nothing could down-weight a bad memory); **files-seeded recall** query (not bare phase label); **steward /distill Phase 0** runs memory_consolidate(dryRun->apply,decay30)+memory_forget (closes 82-store/0-cleanup bloat, executes M5 for the first time); **memory_update on verify-on-recall** (no duplicate-on-staleness); goal_anchor + memory_link supersession. Per-specialist broad recall consciously DEFERRED (would reopen the M4 slice-vs-recall model v2.5.0 settled). **PROGRAM COMPLETE: "what about memory / code search" delivered as v2.11 (code-search MCP wiring — was unused, now in code-heavy agents + onboard index) + v2.12 (memory read-loop + lifecycle).**
- **2026-07-14 — v2.11.0 RELEASED** (both repos, tags ancestors of main, GH releases live; regen-before-push, 421 tests green): **Code-search MCP wiring** (memory+code-search program part 1/2). Audit found the code-search MCP effectively unused — structural tools (code_symbols/code_references/code_outline) in ZERO agents, code_index never built, ~51 agents grep-reimplemented the index. Fix: new shared `## Code search` block (blocks/code-search.md + compact, registered in build-agents.mjs) + CODE_SEARCH.md reference, inlined into coding-agent/dead-code-detector/entry-point-tracer/component-mapper/pattern-consistency-checker; dead-code uses code_references (real graph) not grep count; explore traces call-chains via code_references; onboard Step 0 builds index once; challenger ranks code-search above grep; examples/opencode.json registers the MCP. Mandatory code_index()-then-grep-fallback contract. Engine verified capable (mtime-gated + FTS fallback). **NEXT: v2.12 = memory read-loop + lifecycle** (memory_feedback after recall, files-seeded recall in sdlc-lead, steward runs memory_consolidate+memory_forget, goal_anchor, memory_update on verify-on-recall, memory_link supersession; per-specialist broad recall consciously DEFERRED to not reopen the M4 slice-vs-recall model).
- **2026-07-14 — v2.10.0 RELEASED** (both repos, tags ancestors of main, GH releases live; regen-before-push, 421 tests green): **Agent-wiring audit Tiers 2-3** (staged plan part 3/3 — COMPLETES the v2.8→v2.10 program). Verification loops: migration-planner (was thinnest infra agent) gains Input Contract + Context7 DDL verify + **Step 5 scratch/shadow-DB reversibility proof** (apply→verify→rollback→verify-back-to-OLD→re-apply, "ASSERTED-not-TESTED" required if no scratch DB) + challenger for destructive DDL + guide routing; reliability-engineer must **run a real smoke load** (paste actual req/s/P95/error-rate) or mark §5 NOT MEASURED. **Challenger-gate parity**: tailored gates on db-architect/api-designer/data-steward/reliability-engineer/migration-planner (6 of 7 high-stakes producers had none). Root+consistency gates: **new `validate-flows.sh`** (flows.md = design-chain root, was the only root ungated) + validate-design-tokens.sh wired into Phase-3 GATE_VALIDATORS with red/green fixtures; ux-engineer references tokens.json (no palette fork); a11y Phase-4 ownership boundary; gameplay-engineer names Context7. **PROGRAM COMPLETE: the "look at all remaining agents / ai+design+ux / figma" request delivered as v2.8 (Tier 1 correctness: AI handoff + memory-audit completion on 7 missed specialists + regression gate test-memory-writeback.ts + dead-end handoffs), v2.9 (Figma design-source adapter), v2.10 (Tier 2-3 verification/gates).**
- **2026-07-14 — v2.9.0 RELEASED** (both repos, tags ancestors of main, GH releases live; regen-before-push, 421 tests green): **Figma design-source adapter** (staged plan part 2/3). `scripts/figma/{figma.mjs,figma.sh}` — `pull` a Figma file (X-Figma-Token) → normalized `docs/design/figma-snapshot.json` (variables→tokens/hex, components→inventory, top-level frames→screens); `derive-tokens` → `docs/design/tokens.json` in design-system-lead's shape, reporting keys Figma didn't supply (derived, not invented). **Same pattern as the Jira adapter** — external source via normalized snapshot, `tokens.json` stays SoT for the build, one-way Figma→code, graceful fallback when no FIGMA_TOKEN. Closes the prose-only gap (design-system-lead derives from Figma when a snapshot exists; frontend-design points at the real adapter). New offline gate `validate-design-tokens.sh` (dropped-token/snapshot-without-tokens/value-drift; skips without a snapshot). Optional Figma Dev Mode MCP for live design→code. 6 mocked-REST cases (test-figma-adapter.ts Pass 42). **NEXT: v2.10 = Tier 2+3** (migration-planner apply/rollback/re-apply verify loop + sibling-spec upgrade, reliability-engineer load-run step, validate-flows.sh, STYLE_GUIDE↔tokens.json reconcile, challenger-gate parity for db-architect/api-designer/data-steward/reliability-engineer/migration-planner, browser-testing one-contract, a11y ownership, gameplay-engineer Context7, migration-planner in guide routing).
- **2026-07-14 — v2.8.0 RELEASED** (both repos, tags ancestors of main, GH releases live; regen-before-push, 415 tests green): **Agent-wiring audit Tier 1** (user: "look at all the remaining agents… what about ai/design/ux… figma"). Two-agent audit of the full expert set. (1) **Fixed broken AI handoff** — llm-integration-engineer→coding-agent: coding-agent's input contract had ZERO ref to `docs/design/llm/`, LLM design stranded; now coding-agent reads LLM_DESIGN as required (BLOCKED if LLM feature has none), owasp-llm-checker cross-checks designed-vs-implemented enforcement. (2) **Completed the v2.5.0 memory audit** — it had MISSED 7 producer specialists (architecture-designer +full manifest from a stub, security-auditor, semgrep-runner, 4 sdlc/onboard) because agent-file coverage was never gated; **shipped regression gate `test-memory-writeback.ts` Pass 41** (66 producers checked, proven to bite, orchestrators exempt). (3) **Closed 2 dead-end handoffs** — content-designer microcopy→coding-agent/i18n, end-user-simulator friction→FIX_BACKLOG. **STAGED PLAN (user chose "all of it, staged"): v2.9 = Figma adapter (Figma→tokens.json, same pattern as Jira adapter), v2.10 = Tier 2+3 (migration-planner apply/rollback verify loop, reliability-engineer load-run, validate-flows.sh + validate-design-tokens.sh, STYLE_GUIDE↔tokens.json reconcile, challenger-gate parity for 6 producers, browser-testing one-contract, a11y ownership).**
- **2026-07-14 — v2.7.0 RELEASED** (both repos, tags ancestors of main, GH releases live; regen-before-push, 413 tests green): **Jira integration COMPLETE** ("i want a complete program" — build on `feat/jira-complete`, merged+branch-deleted). Closes the four gaps that made v2.6.0 partial: (1) **convergence keystone `syncState`** — `reconcile` now drains the outbox THEN converges Jira to plan-state, the any-writer catch-all (conductor's in-process lifecycle calls + manual plan.json edits converge with NO per-caller emit hooks; architecturally correct over threading emits everywhere); (2) **conductor auto-mirror** — `mirrorJira()` runs `reconcile` after pick-up + accept, gated on TRACKER_BACKEND=jira, best-effort (Jira outage never breaks the run); (3) **Jira Cloud** backend `JIRA_FLAVOR=cloud` (v3/ADF/email+token/accountId/native parent field; guards compare flavor-correct identity) behind the same interface as DC; (4) **SDLC + install wiring** — /reflow documents sync-plan-then-mirror, install.sh prints JIRA_* setup for DC+Cloud. 12 mocked-REST cases (convergence idempotence + Cloud auth/ADF/accountId). Also gitignore now covers `.worktrees/`+`.tmp-*` (the v2.6.0 stray-add fix). NOTE: conductor.test.mjs opencode-spawn tests fail in THIS env (no opencode binary) — pre-existing, not in `npm test`, unrelated.
- **2026-07-14 — v2.6.0 RELEASED** (both repos, tags ancestors of main, GH releases live; regen-before-push, 411 tests green): **Jira Data Center adapter** (design→approval→build on `feat/jira-adapter`, merged to main). `scripts/jira/{jira.mjs,jira.sh,jira.config.sample.json}` projects the six internal lifecycle verbs + sync-plan/close-epic/pull/reconcile/doctor onto Jira DC REST v2 (Bearer PAT). **plan.json stays SoT; Jira = mirrored ledger.** SDLC hygiene enforced on both surfaces: grab-issues-not-epics, epic-closes-when-children-done, no-double-grab (WIP=1 + cross-surface assignee guard), maker≠verifier, idempotent Epic-Link + "is blocked by" links from `depends_on`, lane→Component. **Lossless degradation** = the ONE SoT-engine touch is a synchronous Jira-free `appendOutbox()` in `tickets.mjs` after savePlan (no-op without a backend); backend-agnostic `scripts/lib/lifecycle-outbox.mjs` holds the durable outbox; `jira.sh reconcile` idempotently replays (answers the repo's own flagged non-atomic dual-write, LESSONS.md:29). New offline-safe gate `validate-jira-hygiene.sh` (skips unless TRACKER_BACKEND=jira). 10 mocked-REST cases (`test-jira-adapter.ts`, no live Jira in CI). Cloud (v3/ADF/email+token) = follow-up behind same interface. Docs: DESIGN_JIRA_ADAPTER.md + references/jira-adapter.md. Also gitignored executor worktrees + test scratch dirs (a stray `git add -A` had swept them in — caught + corrected pre-push).
- **2026-07-14 — v2.5.0 RELEASED** (both repos, tags ancestors of main, GH releases live): expert-wiring audit ("any other experts half-wired? validate all") + fixes. (1) **Memory write-back now universal** — per MEMORY_PRIMER M4 specialists don't recall (lead hands a slice) but MUST `memory_store` durable findings; only coding-agent did, so 44/45 agents' decisions evaporated at session end. Added required `## Memory written` Completion-Manifest section (BOUNDED_TASK_CONTRACT Rule 6 + HANDOFF_TEMPLATES), gated it in validate-completion-manifest.sh ("None — nothing durable" valid), synced 57 inline-manifest agents + green/red fixtures + test-truthful-completion.ts. (2) **Corrected the v2.4.0 coding-agent edit** that wrongly said memory_recall → packet-slice+store. (3) **reliability-engineer Context7** clause added (ships runnable k6/Locust/chaos code). (4) **migration-planner manifest restored** (`## Files modified` + mandatory `Tracker updated:` line were missing → validate-tracker-fresh gap). Audit cleared as NON-gaps: research access uniform where needed (security-auditor delegates CVE currency to dependency-auditor), Challenger correctly wired (v2.4.0), cluster findings consumed by synthesizers. 401 tests green; all validators + agents:check clean.
- **2026-07-14 — v2.4.0 RELEASED** (both repos): loop/gate correctness + coding-agent hygiene (from a 2-audit review of the per-ticket loop vs the completed upgrade plan). (1) Challenger wired into the coding wave (phase-4/feature Round 2b + validate-challenger-gate.sh in phase-4 gate) — was only at design/release gates. (2) Fixed fix-verify.mjs iteration classifier (movement-based per doc: OSCILLATING=regressed, PROGRESSED=all-closed+new, STALLED=survives, CLEARED) — it was count-based + never got `regressed`; test encoded the bug, now fixed + new cases. (3) Loop tier-aware/class-driven not flat-3 (6/12 ceiling); O2.5 contract-conformance in re-verify. (4) coding-agent told about memory MCP + validate-tech-stack now an enforced handoff gate + pre-code anchored (Context7/anti-slop/research/tests-alongside/strict-handoff already present). 401 tests green.
- **2026-07-14 — v2.3.0 RELEASED** (both repos): (1) handoff delivery is now a DOCUMENT the specialist reads (`docs/work/HANDOFF_<agent>.md`) + a pointer to the user (open /skill, read the doc), not a pasted block — reworked HANDOFF_TEMPLATES/EXECUTOR_SELECTION/sdlc-lead + 59 per-handoff headers. (2) Delegation-discipline hardening from a full audit: SDLC lead never does specialist work inline — feature/improve inline-exploration → HANDOFF to app-cartographer; onboard health-coordinator inline guard (sub-checks stay HANDOFFs, only synthesize HEALTH_ASSESSMENT from reports); anti-inline guards on post-code review/security (phase-4/5/feature); allow-list reconciled. Audit verdict: rule now UNIVERSAL across init/feature/improve/onboard. 400 tests green.
- **2026-07-14 — v2.2.2 RELEASED** (both repos): fixed SDLC orchestrators calling specialists DIRECTLY (Executor B subprocess / A Task-tool) in interactive opencode instead of emitting a HANDOFF block telling the user which agent to open + what to submit. Root cause: executor selection keyed on has_task_tool/opencode_cli + T30.10 preferred subprocess in the TUI. Fix: EXECUTOR_SELECTION.md reworked so **autonomy is primary** — interactive → always Executor C (HANDOFF for the user); A/B programmatic dispatch is auto-only. Aligned 14 agent/protocol files. (Grep validator gate evaluated but rejected — too false-positive-prone; the reworked source-of-truth doc is the guard.) 400 tests green.
- **2026-07-14 — v2.2.1 RELEASED** (both repos): upgrade migration for the v2.2.0 pullmd removal. `scripts/migrate-remove-pullmd.sh` (wired into install.sh, auto-heals on upgrade) removes the stale `mcp.pullmd` entry from opencode.json (with backup), stops/removes the pullmd containers, `--purge` deletes the clone. Idempotent no-op on fresh installs. Pass 39 test, 400 tests green. Healed this machine's live config.
- **2026-07-14 — v2.2.0 RELEASED** (both repos): removed the external **pullmd** Docker dependency — web research now runs on our own in-house **bpm-pull** (playwright-search v0.3.0, zero-dep fetch→extract→markdown + Playwright fallback; verified live with the external service down). Stripped ~370 lines from install.sh, the pullmd MCP from opencode.json, and all defunct pullmd_read_url/read_url/get_share tool refs from the research docs. **Retained external by design: Context7, playwright-mcp, Semgrep.** Remaining: mempalace → in-house bpm-memory-mcp (deferred). 398 tests green.
- **2026-07-13 — v2.1.0 RELEASED** (both repos: tags `v2.1.0` ancestors of main, GitHub releases live). Driven via the supervised sonnet auto-loop (13 waves, ~35 clean PRs, zero garbage) from 36%→43/45. Five of six modules complete (M26/M27/M28/M29/M30); **M31 partial**: T31.2 **waived** (Jarvis-dependency, deferred v2.1.x), T31.8 **O3 step-1 pause-census proven live** on LM Studio qwen3-coder-next @127.0.0.1 (interactive=1/auto=1/never-auto=1; also found+fixed a false-green in `pause-census.mjs`), O3 steps 2 (2h soak) + 3 (eval triad) deferred v2.1.x. User docs re-synced (41 skills / 71 validators, `/vault` documented, catalog +vendor-provenance/+GUIDE_CAPTURE). 398 tests green. **Remaining release step (deferred): SW-R1 Shipwright `import-content.mjs` resync — belongs to Shipwright's resume flow (paused repo has uncommitted work; don't resync into it prematurely).**
- 2026-07-12 — v2.0.0 released; amplifier auto-conductor launched on R1; tracker created.
