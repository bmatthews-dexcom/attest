# Troubleshooting

A valid table, followed by an orphaned data row (the historical bug class:
a `|`-delimited row separated from the real table by a blank line, with no
header/separator row of its own).

| Problem | Fix |
|---------|-----|
| Build fails | Run `npm install` |

| Tests hang | Increase the timeout |
