# flask-sqli — eval fixture

Tiny parcel-locker pickup API with two PLANTED defects. Do not fix them — the
eval suite asserts the pipeline finds them. Expected findings live in
`evals/expectations/flask-sqli.json`.
